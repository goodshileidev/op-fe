# Claude Code 快速开发指南

> 本文件为 Claude Code AI 助手提供项目快速参考和当前任务指引（短期记忆）
>
> **详细项目知识请参阅**: @CLAUDE.md (长期记忆文档)

**最后更新**: 2025-11-26 12:00
**更新者**: claude-sonnet-4-5 (claude-sonnet-4-5-20250929)

---

## 🎯 项目概览

**项目名称**: uniform-fe (一体化表单管理平台)
**内部代号**: op-fe-66
**当前分支**: `master`
**主分支**: `master`
**开发端口**: 8000

### 核心特性

- 🎨 **动态表单引擎**: 支持变量化表单设计和动态渲染
- 📋 **模板版本控制**: 完善的表单模板和文档模板版本管理
- 🔐 **细粒度权限**: 基于菜单代码的权限控制，支持业务线隔离
- 🔔 **智能通知系统**: 灵活的通知配置和完整的回执记录
- 📊 **数据可视化**: 基于 @antv/l7 的地理可视化展示
- 🚀 **富文本编辑**: 集成 Quill 编辑器，支持表格和自定义变量

---

## 📦 技术栈

- **框架**: React 18.2 + TypeScript 4.9 + Umi Max 4.1
- **UI 组件库**: Ant Design 5.12 + Pro Components 2.6
- **包管理器**: pnpm
- **国际化**: i18next + Umi locale
- **富文本编辑**: Quill 2.0.2

---

## 📁 项目结构（简化）

```
op-fe-66/
├── src/
│   ├── common/              # 通用模块
│   │   ├── api/            # API 接口定义（按模块分类）
│   │   ├── service/        # 业务服务层
│   │   ├── data_type/      # TypeScript 类型定义
│   │   └── code_list/      # 代码表/枚举
│   ├── components/          # 公共组件
│   ├── pages/               # 页面组件
│   │   ├── form/           # 表单管理
│   │   ├── template/       # 模板管理
│   │   ├── operation/      # 业务操作
│   │   └── system/         # 系统管理
│   ├── lang/                # i18next 国际化
│   └── app.tsx              # 运行时配置
├── docs/                    # 项目文档（标准8个子目录）
│   ├── 01-指引/            # 项目指引、规范、检查清单
│   ├── 02-需求/            # 需求文档
│   ├── 03-架构/            # 系统架构设计
│   ├── 04-设计/            # 详细设计文档
│   ├── 05-实现/            # 实现文档、变更日志
│   ├── 06-测试/            # 测试文档、验证报告
│   ├── 07-交付/            # 交付文档、发布说明
│   └── 99-知识/            # 知识库、FAQ、故障排查
├── CLAUDE.md                # 详细项目理解文档（长期记忆）
├── CLAUDES.md               # 本文件（短期记忆）
└── README.md                # 项目说明
```

---

## 🛠️ 快速命令

### 开发环境

```bash
pnpm install          # 安装依赖
pnpm dev              # 启动开发服务器（端口 8000）
pnpm start:test       # 测试环境
pnpm start:no-mock    # 无 Mock 模式
```

### 代码质量

```bash
pnpm lint             # ESLint 检查
pnpm lint:fix         # 自动修复 ESLint 问题
pnpm tsc              # TypeScript 类型检查
pnpm prettier         # Prettier 格式化
```

### 构建相关

```bash
pnpm build            # 生产构建
pnpm preview          # 预览生产构建
pnpm analyze          # 分析包大小
```

### 快速验证

```bash
# 完整验证套件
pnpm tsc && pnpm lint && pnpm test && pnpm build
```

---

## 🔐 核心规范

### 必须遵守的10条铁律

1. **🇨🇳 简体中文优先** - 所有交流、文档、注释使用简体中文
2. **📁 文档集中管理** - 所有文档保存到 `docs/` 的8个标准子目录
3. **📝 元信息完整** - 文档包含完整元信息（时间戳精确到分钟）
4. **🏷️ 源码功能标注** - 代码包含功能说明和文档引用
5. **✅ 验证优先** - Edit → Read → 确认（强制流程）
6. **📐 TypeScript 严格** - 避免 `any`，使用 `interface`
7. **⚛️ React 最佳实践** - 函数组件 + Hooks
8. **💾 完整功能提交 Git** - 每个完整功能立即提交
9. **📊 数据可追溯** - 所有数字和结论说明来源
10. **🔍 诚实透明** - 不确定时明确说明

> 详细说明：@CLAUDE.md

### TypeScript 规范

```typescript
// ✅ 使用 interface 定义数据类型
export interface DocumentType {
  documentId: string;
  templateName: string;
}

// ✅ 避免使用 any
const handleData = (data: DocumentType | null) => { ... }

// ✅ 明确的函数类型
const fetchDocument = async (id: string): Promise<DocumentType> => { ... }
```

### React 组件规范

```typescript
// ✅ 函数组件 + Hooks
export const DocumentCard: React.FC<DocumentCardProps> = ({ document, onEdit }) => {
  const { t } = useTranslation();
  return <Card>...</Card>;
};

// ✅ 自定义 Hook
export const useDocumentData = (documentId: string) => {
  const [data, setData] = useState<DocumentType | null>(null);
  // ...
  return { data, loading, error };
};
```

### 文件命名规范

- **页面文件**: `{entity}_table.tsx`、`{entity}_edit_page.tsx`、`{entity}_detail_page.tsx`
- **组件文件**: `{entity}_search.tsx`、`{entity}_validate.tsx`
- **数据文件**:
  - API: `src/common/api/{module}/{entity}.ts`
  - Service: `src/common/service/{module}/{entity}.ts`
  - Type: `src/common/data_type/{module}/{entity}.ts`

### 导入路径规范

```typescript
// ✅ 使用绝对路径（@/ 前缀）
import { DocumentType } from '@/common/data_type/form/document';
import { DocumentService } from '@/common/service/form/document';
import SuperTable from '@/components/common/super-table';

// ❌ 避免复杂的相对路径
// import { DocumentType } from '../../../common/data_type/form/document';
```

---

## 🚀 标准工作流程

### 完整开发流程（严格按顺序）

```
1. 分析需求
   ↓ 明确理解用户需求，使用 AskUserQuestion 询问不明确之处

2. 任务规划
   ↓ 使用 TodoWrite 工具创建任务清单

3. 实施开发
   ↓ 按计划逐步实现，遵循代码规范

4. 代码验证
   ↓ Edit → Read → 确认（强制）

5. 质量检查
   ↓ pnpm lint && pnpm tsc

6. 功能验证
   ↓ 浏览器中测试（如适用）

7. 文档更新
   ↓ 重大变更更新 CLAUDE.md 或创建文档

8. 代码提交
   ↓ 使用规范的 commit message
```

---

## 📝 Git 提交规范

### Commit Message 格式

```
type(scope): 简短描述

详细说明：
- 变更内容
- 变更原因
- 影响范围

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>
```

### Type 类型

- `feat` - 新功能
- `fix` - Bug 修复
- `refactor` - 重构
- `perf` - 性能优化
- `docs` - 文档更新
- `style` - 代码格式
- `test` - 测试相关
- `chore` - 构建/工具/依赖更新

### Scope 范围示例

- `form` - 表单相关
- `template` - 模板相关
- `system` - 系统管理
- `notification` - 通知系统
- `component` - 组件

---

## 🔧 常用模式

### API 调用模式（三层架构）

```typescript
// 1. API 定义（src/common/api/form/document.ts）
export const getDocumentById = (id: string) => {
  return request({ url: `/api/form/document/${id}`, method: 'GET' });
};

// 2. Service 封装（src/common/service/form/document.ts）
export class DocumentService {
  static async fetchById(id: string) {
    const response = await getDocumentById(id);
    return response.data;
  }
}

// 3. 页面使用
const DocumentPage = () => {
  const [document, setDocument] = useState<DocumentType | null>(null);
  useEffect(() => {
    const fetchData = async () => {
      const data = await DocumentService.fetchById(id);
      setDocument(data);
    };
    fetchData();
  }, [id]);
};
```

### ProTable 使用模式

```typescript
<ProTable<DocumentType>
  columns={columns}
  request={async (params) => {
    const { list, total } = await DocumentService.fetchList(params);
    return { data: list, total, success: true };
  }}
  rowKey="documentId"
/>
```

### 国际化模式

```typescript
// 1. 定义翻译（src/lang/zh/total/document.ts）
export default {
  documentNo: '文档编号',
  templateName: '模板名称',
};

// 2. 使用翻译
const { t } = useTranslation();
const label = t('document.documentNo');
```

---

## 🐛 常见问题快速解决

| 问题 | 解决方案 |
|------|---------|
| 类型导入报错 | 使用 `@/` 前缀导入 |
| API 调用失败 | 检查 `config/proxy.ts` 和后端服务 |
| 国际化文本未显示 | 检查翻译文件和键名 |
| 权限检查问题 | 查看 `src/escape_authcheck.ts` |
| 构建失败 | `pnpm tsc` 检查类型，`pnpm lint` 检查规范 |

> 详细问题排查：@docs/99-知识/faq.md

---

## 📅 当前任务和状态

### 当前重点：通用化改造 v3.0

项目正在从**港口航运专用系统**向**跨行业通用数据收集平台**演进。

**核心文档**:
- @docs/02-需求/PRODUCT_TRANSFORMATION_PLAN.md - 产品化改造方案（⭐ 重要）
- @docs/05-实现/KICKOFF_TASKS.md - Week 1 实施任务清单

**改造要点**:
1. ✅ 字段动态化 - 将行业特定字段改为可配置
2. ✅ Connector 架构 - 支持外部系统集成
3. 🚧 数据库改造 - 新增场景配置表
4. 🚧 前端重构 - 动态字段渲染

---

## 📚 文档体系

### 双文档体系

本项目采用双文档体系，分别服务于不同的使用场景：

#### 1. CLAUDES.md（本文档）- 短期记忆

**定位**: 快速开发指南，聚焦于当前任务和开发流程

**适用场景**:
- 日常开发工作
- 快速查找命令
- 了解工作流程
- 代码规范参考

**特点**: 简洁实用，聚焦当前任务

#### 2. CLAUDE.md - 长期记忆

**定位**: 详细的项目技术文档，提供深入的架构和业务知识

**适用场景**:
- 理解项目架构
- 学习业务领域
- 技术决策参考
- 深度开发指导

**特点**: 详细全面，深入解析

#### 3. docs/ - 专题文档目录

**标准8个子目录**（必须全部存在）:

```
docs/
├── 01-指引/  ← 代码模式、检查清单、开发指引
├── 02-需求/  ← 需求文档、产品规划
├── 03-架构/  ← 系统架构设计
├── 04-设计/  ← 详细设计文档
├── 05-实现/  ← 实施任务、变更日志
├── 06-测试/  ← 测试策略、验证报告
├── 07-交付/  ← 部署计划、发布说明
└── 99-知识/  ← FAQ、故障排查
```

### 文档使用流程

```
开始新任务
    ↓
读 CLAUDES.md（了解工作流程和命令）
    ↓
需要深入了解？
    ↓
读 CLAUDE.md（理解架构和业务）
    ↓
查找专题文档？
    ↓
读 docs/（查看具体实施方案）
```

---

## 📖 核心文档快速访问

### 规范和指引

- **@CLAUDE.md** - 项目详细指南（长期记忆）
- **@docs/01-指引/code-patterns.md** - 代码模式和最佳实践
- **@docs/01-指引/checklist.md** - 完整检查清单

### 需求和设计

- **@docs/02-需求/PRODUCT_TRANSFORMATION_PLAN.md** - 产品化改造方案
- **@docs/02-需求/TRANSFORMATION_OVERVIEW.md** - 改造方案概览
- **@docs/04-设计/** - 详细设计文档（重构相关）

### 实施和交付

- **@docs/05-实现/KICKOFF_TASKS.md** - 实施任务清单
- **@docs/05-实现/implementation-roadmap.md** - 实施路线图
- **@docs/07-交付/10-deployment-plan.md** - 部署计划

### 知识库

- **@docs/99-知识/faq.md** - 常见问题解答
- **@README.md** - 项目说明

---

## 🔒 质量保证提醒

### 金句

> "没有 Read 验证的 Edit，就是薛定谔的修改"
> "报告进度用数字（X/Y），不用'全部'或'所有'"

### 核心原则

1. **诚实第一** - 宁可不完整，也要准确
2. **验证优先** - 每次 Edit 后必须 Read 验证
3. **逐项报告** - 用数字报告进度（X/Y）

---

## 💡 快速提示

### 开发前检查

- [ ] 拉取最新代码
- [ ] 确认需求和验收标准
- [ ] 创建 todo 清单

### 开发中检查

- [ ] 遵循 TypeScript 和 React 规范
- [ ] 使用 `@/` 前缀导入
- [ ] 避免使用 `any` 类型
- [ ] 实时更新 todo 状态

### 提交前检查

- [ ] **Edit → Read → 验证**（强制）
- [ ] pnpm lint（代码规范）
- [ ] pnpm tsc（类型检查）
- [ ] 更新文档
- [ ] 规范的 Git 提交

---

**最后更新**: 2025-11-26 12:00
**Claude Code 版本**: Sonnet 4.5 (claude-sonnet-4-5-20250929)
**项目版本**: 6.0.51-beta.2

**文档体系**: 遵循 Claude Code 官方指南的双文档体系（CLAUDE.md + CLAUDES.md）
